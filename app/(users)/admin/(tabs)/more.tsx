import { View } from "react-native";

/** Placeholder for the More tab; tab press is intercepted to open the admin sheet. */
export default function AdminMoreTab() {
  return <View className="flex-1 bg-gray-100" />;
}
