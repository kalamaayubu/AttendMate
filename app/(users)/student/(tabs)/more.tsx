import { View } from "react-native";

/** Placeholder for the "More" tab; navigation is prevented — the sheet opens from tab listeners. */
export default function MoreTab() {
  return <View className="flex-1 bg-gray-100" />;
}
